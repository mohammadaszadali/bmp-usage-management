apiVersion: 1.0
logging:
    level:
        com:
            assignment:
                springboot:
                    mongo: DEBUG
    pattern:
        console: '%d{yyyy-MM-dd HH:mm:ss} - %msg%n'
server:
    port: 8899
spring:
    application:
        name: bmp-usage-management-business-service
    data:
        mongodb:
            database: BMP_USAGE_MANAGEMENT
            host: 172.29.1.114
            port: 27017
